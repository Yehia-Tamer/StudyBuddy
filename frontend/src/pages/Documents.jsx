export default function Documents() {
  return (
    <div style={{ padding: 32 }}>
      <h1>Documents</h1>
      <p style={{ color: 'var(--muted)', marginTop: 8 }}>
        Dashboard coming next — this is a placeholder so routing + auth can be tested end to end.
      </p>
    </div>
  );
}